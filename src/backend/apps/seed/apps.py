"""App configuration for the seed app."""

from django.apps import AppConfig


class SeedConfig(AppConfig):
    """Configuration for the seed app (development-only demo data)."""

    name = "apps.seed"
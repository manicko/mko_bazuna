id: task_074_verify_pii

title: Verify — PII/consent cascade & bot blocking

type: verification

status: pending

depends_on:
  - task_034_pii_chat_id
  - task_035_withdraw_route
  - task_036_decline_blocks_login
  - task_039_harddelete_media
  - task_040_withdraw_fsm_cleanup

verifies:
  - task_034_pii_chat_id
  - task_035_withdraw_route
  - task_036_decline_blocks_login
  - task_039_harddelete_media
  - task_040_withdraw_fsm_cleanup

verification_steps:
  - build: "docker compose build"
  - test: "uv run pytest src/backend/apps/users/tests/ src/telegram_bot/tests/ -q"
  - smoke_check: "withdrawn user blocked by bot (chat_id lookup); DECLINE blocks dashboard login + non-browse bot commands; consent/withdraw/ reachable; hard-delete removes media files; mid-FSM DRAFT photos purged"

pass_criteria:
  - withdrawn/revoked identity cannot regain bot access
  - declined users blocked from seller login/actions
  - withdrawal erases media + DRAFT photos
  - withdraw route/admin action reachable

failure_action: return task_034_pii_chat_id to rework

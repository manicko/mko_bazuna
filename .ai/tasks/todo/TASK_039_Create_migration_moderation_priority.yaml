id: TASK_039_Create_migration_moderation_priority

title: Create migration for AdModerationPriority model

priority: medium

depends_on:
  - TASK_019_Create_AdModerationPriority_model

source_reference: .ai/plans/02/Enhanced Moderation Tooling/plan.md
source_section: T11: Create Migration for AdModerationPriority

description: >
  Create database migration for AdModerationPriority model in
  apps/moderation/migrations/0003_ad_moderation_priority.py.

goals:
  - Generate migration for ad_moderation_priority table
  - Include indexes on priority_level, base_score, escalation_required

files:
  - path: src/backend/apps/moderation/migrations/0003_ad_moderation_priority.py

verification_steps:
  - uv run python src/backend/manage.py makemigrations --check --dry-run moderation
  - uv run python src/backend/manage.py migrate --plan

risk_level: medium
risk_mitigation: >
  New table; no existing data affected.
  One-to-one relation to Ad is optional until signal fires.
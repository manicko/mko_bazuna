id: task_071_verify_boot

title: Verify — all processes boot & config fail-fast

type: verification

status: pending

depends_on:
  - task_006_fix_container_boot
  - task_007_fix_bot_import_order
  - task_008_bot_token_failfast
  - task_009_postgres_password_failfast
  - task_010_web_restart_policy

verifies:
  - task_006_fix_container_boot
  - task_007_fix_bot_import_order
  - task_008_bot_token_failfast
  - task_009_postgres_password_failfast
  - task_010_web_restart_policy

verification_steps:
  - build: "docker compose build"
  - test: "uv run pytest -q"
  - smoke_check: "python -c 'import config, apps, telegram_bot' && bot boots to event loop without ImproperlyConfigured; gunicorn starts; missing BOT_TOKEN/POSTGRES_PASSWORD fail fast"

pass_criteria:
  - image builds and all three processes import correctly
  - web + bot share one secret-loading mechanism
  - missing credentials raise at startup, not silent exit 0

failure_action: return task_006_fix_container_boot (and dependent boot tasks) to rework

id: TASK_040_Create_migration_daily_ad_metrics

title: Create migration for DailyAdMetrics model

priority: medium

depends_on:
  - TASK_025_Create_DailyAdMetrics_model

source_reference: .ai/plans/02/Analytics Improvements/plan.md

description: >
  Create database migration for DailyAdMetrics model in analytics migrations.

goals:
  - Generate migration for daily_ad_metrics table
  - Include unique constraint on (ad, date)
  - Include index on (date, -views_count)

files:
  - path: src/backend/apps/analytics/migrations/0003_daily_ad_metrics.py

verification_steps:
  - uv run python src/backend/manage.py makemigrations --check --dry-run analytics
  - uv run python src/backend/manage.py migrate --plan

risk_level: medium
risk_mitigation: >
  New table; no existing data affected.
  Daily rollups can be backfilled.
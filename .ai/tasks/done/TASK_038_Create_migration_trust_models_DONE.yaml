id: TASK_038_Create_migration_trust_models

title: Create migration for Trust models

priority: medium

depends_on:
  - TASK_018_Create_Trust_models

source_reference: .ai/plans/02/Trust Signals System/plan.md

description: >
  Create database migration for SellerTrustScore and SellerVerification models.

goals:
  - Generate migration for seller_trust_scores table
  - Generate migration for seller_verifications table
  - Ensure trust_level choices match TrustLevel enum

files:
  - path: src/backend/apps/trust/migrations/0001_initial.py

verification_steps:
  - uv run python src/backend/manage.py makemigrations --check --dry-run trust
  - uv run python src/backend/manage.py migrate --plan

risk_level: medium
risk_mitigation: >
  One-to-one relations to User are safe.
  Trust scores can be recalculated if needed.
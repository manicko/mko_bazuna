Язык фильтров и сортировки:

Должны меняться при смене языка в настройках.
Нужно перевести названия фильтров на все языки, которые мы используем в базе и при смене языка - фильтры должны менять название


Сортировка: 
1) Нужно отвязать сортировку от кнопки фильтров - сортировка должна применяться без кнопки фильтровать - просто при смене вида сортировки.
2) Блок сортировки нужно вынести визуально отдельно от блока фильтров и она должна отображаться всегда 

Фильтры 
- за основу берем такое же отображение, как у OLX:
1) Блок Features и Listing purpose нужно сделать выпадающим списком с чекбоксами, как на OLX.
2) Блок с фильтром по цене не отображается, хотя был запланирован в предыдущем плане. Нужно понять почему и сделать его. Вид блока точно такой, как у OLX - цена от и до.

Объявления

1) Нужно, чтобы все features были также указаны в тексте объвлений, сейчас в тексте объвялений нет перечисления features каждого объявления. Скорее всего это связано с тем, как сейчас создаются тестовые объявления. Нужно проверить как они создаются и если проблема в том, что они вписаны в тестовые объявления - прописать. Если вписаны, то вывести на сайт. 
В общем в итоге, после обновления контейнеров докер с тестовыми объявлениями, мы должны увидеть features у всех объявлений.
2) Нужно проверить сами features для каждого объявления на адекватность заполнения. Например, у домашних животных и дешевых товаров не может быть фичи - в кредит и т.п.

у меня есть  проблема. 
Я хочу иметь возможность смотреть дизайн сайта в dev режиме. 
Но пустой сайт нельзя нормально оценить:

не видно карточек;
не видно пагинации;
не видно поиска;
не видно фильтров;
не видно длинных заголовков;
не видно разных фотографий;
не видно адаптивности. 

Мне нужно временное фейковое наполнение - нагенерить текста объявлений с фото и т.п. 

Я бы заложил это как отдельный модуль проекта для Demo Data, например, так 

apps/
└── seed/
    ├── management/
    │   └── commands/
    │       └── seed.py
    │
    ├── generators/
    │   ├── users.py
    │   ├── ads.py
    │   ├── images.py
    │   ├── ....py
    │   └── ....py
    │
    ├── fixtures/
    │   ├── categories.json
    │   ├── cities.json
    │   ├── ads.json
    │   └── images/
    │
    └── config/
        └── seed.dev.yaml
но нужно изучить совремнные практики, как правильно

Что должен создавать seed, например, так
python manage.py seed --<number of ads> --<number of users> --<другие параметры>
Тут нужно на основе нашей архитектуры понять как лучше.

python manage.py seed

появляются:
✔ категории
✔ города
✔ пользователи
✔ объявления
✔ фотографии
✔ избранное
✔ просмотры
....

Но лучше проверить, как делают современные проекты

причем, мне нужно, чтобы в dev режиме можно было добавить запуск seed в docker и контейнер и сайт стартовали уже с данными.
Можно так: 
docker compose  --profile seed  up
Но лучше понять, как делают современные проекты.

Я не хочу генерить все на лету, я за то, чтобы часть данных генерировалась на лету (id пользователей, цены и что-то еще например, через Faker), а часть была  создана заранее - fixtures - yaml|json с текстами объявлений, назаваниями и ссылками на фото, сами фото. 